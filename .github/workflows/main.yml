name: CI/CD

on:
  push:
    branches:
      - main

jobs:
  unit-test:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout Repository
      uses: actions/checkout@v3

    - name: Install dependencies
      run: npm install

    - name: Run unit tests
      run: npm test

  build:
    needs: unit-test
    runs-on: ubuntu-latest

    steps:
    - name: Checkout Repository
      uses: actions/checkout@v3

    - name: Install Dependencies
      run: npm install

    - name: Build Website
      run: npm run build

    - name: Install GCC (if needed)
      run: sudo apt-get update && sudo apt-get install -y gcc

    - name: Compile Code
      run: gcc -o myapp main.c   # Thay main.c bằng tên của tệp mã nguồn chính và myapp bằng tên tệp kết quả mong muốn

    - name: Generate Hex File
      run: objcopy -O ihex myapp myapp.hex   # Tạo tệp hex từ tệp nhị phân

    - name: Build Docker image
      run: docker build -t myapp .

    - name: Push Docker image to registry
      uses: docker/build-push-action@v2
      with:
        username: ${{ secrets.DOCKER_USERNAME }}
        password: ${{ secrets.DOCKER_PASSWORD }}
        repository: username/repository
        tag_with_ref: true
